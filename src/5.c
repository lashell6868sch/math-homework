#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main() {
    int num1 = 2;
    int num2 = 3;
    float result = (float)num1 / num2;
    printf("The result is: %f\n", result);
    return 0;
}
